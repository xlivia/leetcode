var copyRandomList = function(head) {
    /**
     * @param {Node} head
     * @return {Node}
     */
    /**
     * // Definition for a Node.
     * function Node(val, next, random) {
     *    this.val = val;
     *    this.next = next;
     *    this.random = random;
     * };
     */

};